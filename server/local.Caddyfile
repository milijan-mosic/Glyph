app.glyph.local {
    tls internal

    handle /api/* {
        reverse_proxy api:5000
    }

    handle {
        reverse_proxy www:3000
    }
}

pgadmin.glyph.local {
    tls internal

    handle {
        reverse_proxy pgadmin:80
    }
}
